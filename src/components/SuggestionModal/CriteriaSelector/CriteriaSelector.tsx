import React from 'react';
import { View } from 'react-native';

export interface CriteriaSelectorProps {
  // Props will be defined later
}

export function CriteriaSelector(props: CriteriaSelectorProps) {
  return (
    <View testID="criteria-selector">
      {/* Implementation will be added later */}
    </View>
  );
}